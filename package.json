{
  "name": "smk-teknovo-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Public landing portal for SMK Teknovo",
  "scripts": {
    "build": "node scripts/validate.js",
    "lint": "node scripts/validate.js",
    "serve": "python3 -m http.server 8080 --directory public",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.22.0"
  }
}
